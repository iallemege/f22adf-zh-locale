# 给 F-22 ADF 这一类游戏做简体中文汉化

面向 1990 年代 DID / Ocean 系 PC 空战模拟，以及后来加上 Direct3D 重制层的 Steam 再版。本教程以 *F-22: Air Dominance Fighter*（Steam app **3146140**，2026 年重制）为完整实例。同族或相近的还有 EF2000、F-22 Total Air War、其它「GDI 画界面 + 自研 8 位字模画简报」的老模拟。

手册译文的用词对齐 DCS 中文飞行手册（以官方《F/A-18C“大黄蜂”抢先体验指南》和 Heatblur《F-14“雄猫”》中文手册为参照）。体例见 `manuals/dcs_style.md`。

---

## 1. 先判断你在汉化哪一种字

不要一上来就「全文替换成 UTF-8」。这类游戏通常同时存在好几条互不相通的文字管道。F-22 ADF 2026 版至少有六条：

| 管道 | 典型文件 | 谁画出来 | 编码 | 能不能直接写汉字 |
| --- | --- | --- | --- | --- |
| GDD gadget 的 `TEXT` / `TITLE` / `TOOLTIP` | `f22data\*.gdd` | 系统 GDI `ExtTextOutA` | 系统 ANSI。中文 Windows 上是 **GBK（CP936）** | 能。字体改成黑体即可 |
| 简报正文、任务目录名 | `briefing\*.txt`、`f22data\*miss*.txt` | 重制层 8 位 walker + **256 格字模缓存**，再 D3D/软件 blit | 原版按 Latin-1 / 西欧 UTF-8（只认 `C2`/`C3`）走 | 不能只改文本。必须挂钩解码器 |
| 主菜单大字 | `pcx\` 里烤进图的点阵 | 直接贴图 | 无 | 要重绘 PCX，或换图 |
| 点阵按钮字 | `icondesc.txt` + `topazpro` 一类点阵字体 | 引擎自己的拉丁点阵 | 拉丁 | 不要写汉字，会碎成乱码方块 |
| exe 硬编码 | `adf.exe` 里的 `MISSION OBJECTIVES` 等 | 同上覆盖层或内部表 | 窄字符串 | 不改 PE 就保持英文 |
| 手册 / 帮助 | `Manual English.pdf`、`Keys Guide English.pdf`、`Help English.chm` | 外部阅读器 | PDF 文本层或扫描 OCR | 另做中文 PDF，不要塞进覆盖层 |

一条经验：界面能出中文，简报仍是 `ÈÎñ` 或问号，说明你只打通了 GDI，没打通 256 格字模那条。反过来也成立。

---

## 2. 编码：GBK、UTF-8、西欧双字节

### 2.1 界面必须是 GBK

1990 年代 Win32 模拟几乎都走 `*A`（ANSI）GDI。在简体中文 Windows 上，ACP 是 936。把界面写成 UTF-8，GDI 会把每个字节当独立 ANSI 字符，出现 `ä»»åŠ¡` 这种典型 UTF-8 被当 Latin-1 的乱码。

读写脚本不要先用 `latin-1` 解码：它永远成功，再 `encode('gbk')` 会把已经读歪的汉字变成 `?`。正确顺序是 **UTF-8 → GBK → CP1252**，都失败再用替换。

### 2.2 重制层不是「现代 UTF-8」

2026 重制在 VA `0x32750`（镜像基址 `0x10000`，无 ASLR，RVA `0x22750`）有一个字节 walker。它只把 `C2 xx` / `C3 xx` 当成西欧 UTF-8（法语、德语、西班牙语的重音字母）。汉字 UTF-8 是三字节 `E4–E9 …`，会被丢掉或拆碎。

GBK 汉字则被当成「原始 Latin-1 单字节」去查 0–255 字模，所以「任务」会变成 `ÈÎñ`（把 GBK 字节按 CP1252 显示的结果）。

因此：**界面 GBK 是对的；简报也要 GBK 存盘，但必须自己解码 GBK，再把字形塞进引擎的 256 格缓存。**

### 2.3 和西欧官方语言撞车

GBK 里不少常用字的首字节就是 `C2`/`C3`：

- 美 = `C3 C0`
- 轮 = `C2 D6`
- 夺 = `C2 E2`

如果先按西欧 UTF-8 吃掉 `C2`/`C3`，这些字会变成 `?`。规则是：**先认 GBK 双字节，再把剩下的交给原版 `C2`/`C3` 解码。**

全角 ASCII（GBK `A3 A1`–`A3 FE`，例如全角冒号 `：`）要折回半角，否则覆盖层按「一个西文字宽」排版，冒号和数字会对不齐。

不要在 GBK 之前先吞 UTF-8 三字节：指针会和双字节失步，后面整行错位（例如「架」变成「揍」）。

---

## 3. 查找键不要翻译

引擎用英文字符串当内部 ID 的地方，译了会直接坏。F-22 ADF 的硬例子：

- `options.cfg` 里的 `FEATURE` 名称，例如 `COCKPIT REFLECTIONS`。译成中文后启动报 `Could not find feature: COCKPIT REFLECTIONS`。
- GDD 里部分窗口名，例如 `WINDOW "Options"`。只译 `TEXT` / `TITLE` / `TOOLTIP TEXT`，不要全局 `quoted_replace`。
- 任务文件名、简报文件名、PCX 文件名保持英文。
- 武器/飞机型号保持原文：AIM-120C、F-15E。界面可以写「AIM-9X“响尾蛇”」，查找表仍是 `AIM-9X`。

替换策略：**最长优先**。先换 `Interface Art Director`，再换 `Interface Art`，否则会留下半截英文。

PowerShell 会把 Python 正则和引号吃坏。词典和替换放在 `.py` 文件里跑，不要写在 `.ps1` 一行里。

---

## 4. 字体

GDI 界面要换支持 GB2312/GBK 的 TrueType。本项目把黑体拷成 `WINFONTS\zh_ui.ttf`，`DEFINE_SYS_FONT` 的 `STYLE` 写成 `"SimHei"`（必须和 TTF 的家族名一致，不是文件名）。

F-22 ADF 的 `CREATE_SYS_FONT` **最多 3 行**。多写引擎会丢字体或回退到拉丁点阵。正确组合：

```text
CREATE_SYS_FONT "winfonts\zh_ui.ttf"
CREATE_SYS_FONT "winfonts\zh_ui.ttf"
CREATE_SYS_FONT "winfonts\UAV-OSD-Sans-Mono.ttf"
```

第三行留给平显/OSD 等宽拉丁字体，不要换成黑体，否则机内数字和符号会变比例。

覆盖层那条管道不走 GDI 整串输出。它在启动时对 `0..255` 每个码位 `ExtTextOutA` 一个字符，`GetCharWidth32A` + `GetDIBits` 烤成 1-bit 字模。汉字不在这 256 格里。做法是：解码到某个空槽（本项目用 index `1`），按 **两倍 `tmAveCharWidth` × `tmHeight`** 的固定格子、`TA_BASELINE` 对齐 `tmAscent` 烤字形。用汉字自身的 em 宽会裁掉短标签最后一个字（「任务类型」「起飞位置」），并上下错位。

---

## 5. 术语：游戏界面可以短，手册必须按 DCS

界面按钮要短，手册要书面。两套词可以略有差别，但不能互相打架。

| 概念 | 界面（短） | 手册（DCS） |
| --- | --- | --- |
| cockpit | 座舱反射 | 驾驶舱 |
| HUD | 平显 | 平视显示器（HUD） |
| wingman | 僚机 | 僚机 |
| EMCON | 电磁控制 | 电磁控制（EMCON） |
| BVR | 视距外 | 视距外（BVR） |
| pickle | 投放 | 投放 |
| Tour of Duty | 战区巡航 | 战区巡航 |
| Quick Combat | 快速作战 | 快速作战（街机对抗模块） |

告警等级按 DCS / 民航习惯：

- WARNING → **警告**
- CAUTION → **注意**
- NOTE → **注**
- Advisory → **提示**

座舱刻字、MFD 按钮上的英文保留。武器型号保留，绰号加中文引号：AIM-9X“响尾蛇”。地名用通行译名（鲁卜哈利沙漠、海米斯穆谢特、曼德海峡），不要自造。

完整对照：`术语表.txt` 与 `manuals/dcs_style.md`。

---

## 6. 推荐工程流程

1. **只把补丁做成独立目录**，不要把整个 Steam 游戏目录初始化成 git。游戏数据受版权保护。本项目的仓库只有 `locale_zh\`。
2. **先拷一份英文备份**，而且只在文件还不存在时拷。否则第二次运行补丁会把已经汉化的文件当成「原文」覆盖备份。
3. **先接通字体和一两个按钮**，确认 GDI 管道出汉字，再铺开词典。
4. **再处理简报**。先写 GBK 正文，再上解码挂钩。只挂钩 `ExtTextOutA` 不够：简报走软件 blit / D3D 字模，不走 GDI 整串。
5. **用真实任务画面验收**，不要只看菜单。短标签（四个汉字）最容易裁切和错位。含 `美` `轮` `夺` 的句子用来测 `C2`/`C3` 冲突。
6. **手册另做**。扫描版 PDF 的 OCR 质量差，按 DCS 体例重排一册中文手册，比在原 PDF 上盖字更干净。

安装本补丁（游戏已在上一级目录时）：

```text
python apply_ui.py
python apply_briefing_bodies.py
python apply_briefing_labels.py
python apply_catalogs_gbk.py
python apply_finish.py
```

把 `gdi_zh\DINPUT8.dll` 拷到与 `adf.exe` 同级。启动后根目录应出现 `gdi_zh.log`。关掉汉字覆盖层：删掉根目录那份 `DINPUT8.dll`。

---

## 7. 界面（GDD）怎么换

GDD 是 DID 的 gadget 描述。显示字符串多半在双引号里。用「只替换引号内、最长优先」的词典，避免碰到窗口类名。

座舱提示在 `huddle\f22.ins` 的 `HINTTEXT` 里，同样按 GBK 写，走 GDI。

主菜单 PCX 和 `icondesc.txt` 的点阵按钮（BRIEFING / OK）保持英文，除非你愿意重绘点阵字库。

---

## 8. 简报覆盖层：必须挂钩解码器

### 8.1 为什么 IAT Hook 不够

`ExtTextOutA` 的 IAT 钩子能修 GDD。简报正文的路径是：

1. 字节 walker（`0x32750`）一次吐一个「拉丁码位」；
2. 字模烘焙（`0x32810`）只有 256 个 1-bit 字形；
3. 绘制用当前字体对象 `0x14582d8` 里 `font+0x12+index*8` 的 `{宽, 高, 位图}`；
4. D3D 路径入口 `0x38720`，软件 blit 给情报简报。

整串汉字从来不会交给 `ExtTextOutA`。

### 8.2 用 DINPUT8 当加载器

`adf.exe` 导入 `DINPUT8.dll` 的 `DirectInput8Create`。在游戏根目录放一份 32 位代理：

1. `GetSystemDirectoryW` 加载系统 `dinput8.dll`，把 `DirectInput8Create` 转发出去；
2. `DllMain` 里对 exe 做 IAT 钩：`ExtTextOutA`、`TextOutA`、`GetCharWidth32A`、`GetTextExtentPoint32A`、`DrawTextA`；
3. 对解码函数和 D3D 字模绘制做 inline hook（`E9` 跳转）。镜像无 ASLR，RVA 可以写死。若入口第一字节已经是 `E9`，说明钩过了，不要再钩。
4. 用 32 位 TCC 或等价工具编译。TCC 会导出 `_DirectInput8Create@20`，游戏找的是未修饰的 `DirectInput8Create`，链接后必须改导出表（见 `gdi_zh\fix_export.py`）。

杀毒软件有时会报代理 DLL。这是这类包装的常见误报；发布时写清楚来源和关闭方法。

### 8.3 解码规则（按试验过的顺序）

1. GBK 全角 ASCII `A3 A1`–`A3 FE` → 半角 `trail-0x80`。
2. GBK 双字节（含 `C2`/`C3` 打头的汉字）→ `MultiByteToWideChar(936)`，字形写入槽 `1`，返回 index `1`。覆盖层按**两个**西文字步前进。
3. 其余交给原版 walker（西欧 `C2`/`C3` 和纯 ASCII）。

D3D 绘制时把汉字画到当前 rendertarget；失败再拷到离屏表面画完 blit 回去。颜色从引擎色表取，掩码在 `RVA_COLORMASK`。

---

## 9. 手册 PDF

Steam 版带两份英文 PDF：

- `Manual English.pdf`：1997 年印刷手册的扫描件（Xerox / PixEdit），约 175 页。有 OCR 文本层，但 `ln`/`I`、`Ouick`、`ta`/`to` 一类错误很多。
- `Keys Guide English.pdf`：重制时用 LibreOffice 排的两页键位表，文本层干净。

汉化标准按 DCS 中文飞行手册，不是攻略站口吻。做法：

1. 抽出文本，按原书目录分章（`manual_src\chapters\`）。
2. 按 `manuals\dcs_style.md` 译成 Markdown：告警框、保留刻字、型号加绰号引号、英制单位保留。
3. 用 `manuals\build_pdfs.py` 生成中文 PDF，复制到游戏根目录：
   - `F-22 ADF 飞行手册 中文.pdf`
   - `F-22 ADF 按键对照 中文.pdf`

不要把 70 MB 的英文扫描件整本上传到公开仓库。译文源文件可以留在 `locale_zh\manuals\`。

帮助 CHM 与手册内容大量重复，优先保证 PDF；CHM 需要另外的 HTML 帮助编译器。

---

## 10. 常见失败

| 现象 | 原因 | 处理 |
| --- | --- | --- |
| 菜单是中文，简报是 `ÈÎñ` | 只改了 GBK，没挂钩 walker | 安装 `DINPUT8.dll` |
| 「美/轮/夺」变成 `?` | `C2`/`C3` 先被西欧 UTF-8 吃掉 | GBK 双字节优先 |
| 「任务类型」少最后一个字 | CJK 槽用了可变 em 宽 | 固定两倍平均字宽 |
| 汉字偏上或偏下 | 用了 `TA_TOP` 或按格子顶对齐 | `TA_BASELINE` + `tmAscent` |
| 启动找不到 feature | 译了 `options.cfg` 的 FEATURE | 只译 `option.gdd` 的显示名 |
| 字体回退成方块 | `CREATE_SYS_FONT` 超过 3 行，或 STYLE 不是 `SimHei` | 见第 4 节 |
| 代理 DLL 加载失败 | 导出名是 `_DirectInput8Create@20` | 跑 `fix_export.py` |
| 主菜单仍是英文 | 字在 PCX 上 | 预期。要改就重绘 |
| 二次运行把备份污染成中文 | 备份逻辑无「已存在则跳过」 | 先判断目标不存在再 `copy` |

---

## 11. 验收清单

- 中文 Windows，系统有黑体。
- 选项、训练、战区巡航、快速作战、多人、ACMI、预警机、讲评的按钮和悬停提示是中文。
- `SHIFT+O` 选项里显示名是中文，但改开关不会报找不到 feature。
- 打开任意简报：标题、任务类型、起飞位置、正文是中文且不裁字。
- 任务目录列表是中文。
- 含「美国」「轮换」「夺取」的句子不出现问号。
- 平显数字仍是等宽拉丁，没有被黑体撑开。
- 根目录有 `gdi_zh.log`，能看到 `inline decode+d3d glyph hooked`。
- 删除 `DINPUT8.dll` 后游戏仍能启动，简报回英文乱码或原西欧路径，界面中文仍在。
- 两份中文 PDF 能用系统阅读器打开，目录和键位与英文版对应。

---

## 12. 法律与发布

补丁只发布你自己写的脚本、代理源码、译文和术语表。不要发布 `adf.exe`、简报原文包、扫描版英文 PDF、语音。README 写明：仅供正版用户把补丁放到自己的游戏目录使用。

代理 DLL 会改进程内函数入口。写清楚用途、关闭方法和杀毒误报，避免被当成来路不明的注入。
